# Core dependencies for OCR and image processing
# No UI dependencies (PyQt6, PyAutoGUI, etc.)

# Environment management
python-dotenv==1.2.1

# Cloud OCR providers
google-cloud-vision>=3.11.0
azure-ai-vision-imageanalysis>=1.0.0
boto3 

# Image processing
opencv-python>=4.10.0
Pillow>=11.1.0  
numpy>=2.2.1

# ============================================
# DEEP LEARNING & ML MODELS
# ============================================
# PyTorch CPU-only (optimizado para Intel)
#NO INSTALAR TORCH NORMAL
#Hacerlo con el siguiente comando:
#pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
torch>=2.8.0
torchvision>=0.21.0

# ============================================
# OCR & TEXT PROCESSING
# ============================================
pytesseract>=0.3.13  

# Transformers (Table Transformer necesita >=4.30.0)
transformers>=4.48.0
# Timm (Vision models - requerido por Table Transformer)
timm>=1.0.12

# Text processing and validation
python-Levenshtein>=0.27.0
unidecode>=1.3.8 

# ============================================
# UTILITIES
# ============================================
# Accelerate (optimiza carga de modelos)
accelerate>=1.2.1

# Logging
structlog==24.1.0

# Configuration
pyyaml==6.0.1

# Testing
pytest==8.0.0

PyPDF2 